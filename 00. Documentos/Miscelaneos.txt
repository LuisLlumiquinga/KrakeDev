Comandos de Bash
pwd --> directorio
cd --> change directory
cd .. --> regresa directorio anterior
clear --> limpia consola
ls - lista carpetas del directorio actual

git config --global user.name LuisLlumiquinga
git config --global user.email lmllumiquinga3@espe.edu.ec

git config --global --list

git clone "url" --> clona el repositorio

GIT HUB
	Cuando se agrega un archivo al directorio de trabajo su estado es UNTRACKED

git commit -m "mensaje" --> crea versiones
git add "nombre.xx" --> agrega el archivo al directorio de trabajo
git status --> estado de los repositorios
git log --> general el historial

Para salir: Esc+":q"

git push --> sube archivos a la nube

git pull origin main --> actualiza desde la nube

****** java ******

	.toFixed(2) --> controlar decimales
	.length --> devuelve # de caracteras del string
	.charCodeAt(2) --> saca codigo ASCI del caracterer
	.charAt(0) --> saca la letra en la posicion dada
	isNaN(nota1) --> para validar NaN
	.push(valor) --> agrega elementos a la cadena de caracteres
	alt + shif + F --> ordena la tabulacion del codigo escrito
	.toString() --> cambia un INT a STRING
	parseInt() -->  cambia strinf a int
	.trim() --> elimina espacios

*** poner pausa ***	
	setTimeout(()=>{
        limpiar();
    }, 2000);

*** objeto vacio ***
	let hola={}

*** arreglo vacio ***
	let hola=[]
	hola.push();

proyecto JAVA
java11-junit5

proyecto MAVEN
webapp-javaee8		**archivo exportado

proyecto MAVEN ULTIMO
webapp-j2ee13
configurar data source (server y context)
configurar web.xml (servlet y servlet-mapping)
copiar las dependencias del pom.xml



IMPORTS
LOGGER --> org.apache.logging.log4j.Logger

--> en el archivo web.xml ver modificaciones